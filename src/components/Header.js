import NavBar from './NavBar';

const Header = () => {
    return <>
        <head>
            {/* To Be Determined */}
        </head>
        <header className="header container-fluid">
            <NavBar />    
        </header>
    </>
}

export default Header;